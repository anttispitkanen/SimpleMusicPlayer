import { nowPlaying } from './index';

describe('playing', () => {
    describe('nowPlaying', () => {

        // it('inits as null', () => {
        //     // expect(nowPlaying(null, {}).toEqual(null));
        // })

        it('does not mean anything', () => {
            expect(0).toEqual(0);
        })

    })
})